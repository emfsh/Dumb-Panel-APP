## 更新内容

- 任务分组与面板进一步同步，任务编辑页可直接选择已有分组。
- 任务页记住分组折叠状态、分组筛选和滚动位置，返回后不再默认全展开并跳回顶部。
- 任务列表改为直接加载完整任务集，减少分组视图里任务偶发缺失的问题。
- 任务置顶入口前置到卡片操作区，常用任务可以更快固定到前面。
- 脚本管理新增本地置顶能力，高频抓包脚本可直接置顶到列表前面。
- 环境变量分组对齐面板多分组接口，支持按多分组读写和筛选。
